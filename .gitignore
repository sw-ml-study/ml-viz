# Rust build artifacts
target/

# wasm-pack output (regenerated by `cargo run --example ml-test-scene`)
yew-app/pkg/

# Blender + generator output — fully regenerable from scenes/*.yaml + templates/
/generated/

# Personal Claude Code permissions (shared settings.json stays committed)
.claude/settings.local.json

# Blender autosave / backup
*.blend1

# Editor / OS detritus
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
*.iml
